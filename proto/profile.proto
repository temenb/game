syntax = "proto3";

import "common/health.proto";
import "common/empty.proto";
import "auth.proto";

package profile;

service Profile {
  rpc Health (common.empty.Empty) returns (common.health.HealthReport);
  rpc Status (common.empty.Empty) returns (common.health.StatusInfo);
  rpc Livez (common.empty.Empty) returns (common.health.LiveStatus);
  rpc Readyz (common.empty.Empty) returns (common.health.ReadyStatus);

  rpc GetProfileByUser (auth.UserIdRequest) returns (ProfileObject);
  rpc UpsertProfile (auth.UserIdRequest) returns (ProfileObject);
}

message ProfileIdRequest {
  string profile_id = 1;
}

message ProfileObject {
  string id = 1;
  string user_id = 2;
  string nickname = 3;
  int64 level = 4;
  int64 rating = 5;
  int64 experience = 6;
}
