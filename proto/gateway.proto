syntax = "proto3";

import "auth.proto";
import "common/health.proto";
import "common/empty.proto";
import "profile.proto";
import "battle.proto";

package gateway;

service Gateway {
  rpc Health(common.empty.Empty) returns (common.health.HealthReport);
  rpc Status(common.empty.Empty) returns (common.health.StatusInfo);
  rpc Livez(common.empty.Empty) returns (common.health.LiveStatus);
  rpc Readyz(common.empty.Empty) returns (common.health.ReadyStatus);

  rpc AnonymousSignIn(auth.AnonymousSignInRequest) returns (auth.AuthObject);
  rpc RefreshTokens (auth.RefreshTokensRequest) returns (auth.AuthObject);
  rpc ViewMyProfile(common.empty.Empty) returns (profile.ProfileObject);

  rpc NewBattle (common.empty.Empty) returns (battle.BattleObject);
  rpc MakeMove (battle.MakeMoveRequest) returns (battle.BattleObject);
}
